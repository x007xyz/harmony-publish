{
  "name": "harmony-app-id",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Create an AGC APP ID via Playwriter (connects to the running Chrome, preserving login state). One-shot script: AI pre-generates parameters, script creates without LLM confirmation.",
  "dependencies": {
    "playwriter": "0.0.56",
    "playwright-core": "1.56.1"
  }
}
